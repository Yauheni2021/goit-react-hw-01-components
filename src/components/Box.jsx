import styled from "@emotion/styled";

import {
    color,
    space,
    layout,
    flexbox,
    grid,
    background,
    border,
    position,
    typography,
    shadow,
} from "@emotion/";

export const Box = styled('div')(
  color,
  space,
  layout,
  flexbox,
  grid,
  background,
  border,
  position,
  typography,
  shadow
);
